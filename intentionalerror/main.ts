let  randomWords:  string[] = ["Ball" , "Racket" , "Puck" , "Bat"]

// by retrieving invalid index of array producing error
//console.log(randomWords[6]);

// error removed
console.log(randomWords[3]);